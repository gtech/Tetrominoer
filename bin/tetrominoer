#!/usr/bin/env ruby
$: << File.join(File.dirname(__FILE__), "..", "lib")
require "tetrominoer"

module Tetrominoer


  o = O.new()
  l = L.new()
  r = R.new()
  i = I.new()
  s = S.new()
  z = Z.new()
  a = [i]
  pg = PossibilityGenerator.new(4,3)
  pg.generate(a)

binding.pry

end
#  l = PieceMatrixGenerator.new(5)

 # l.generate
