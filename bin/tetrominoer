#!/usr/bin/env ruby
$: << File.join(File.dirname(__FILE__), "..", "lib")
require "tetrominoer"

module Tetrominoer


  o = O.new()
  l = L.new()
  a = [o]
  pg = PossibilityGenerator.new(3,3)
  pg.generate(a)

binding.pry

end
#  l = PieceMatrixGenerator.new(5)

 # l.generate
